import React from 'react';
import styles from '../DropDown.module.css';

const UserDropDown = (props) => {
    return (
        <div id={styles.dropDownContainerAnimated} className={styles.dropDownContainer}>
            
        </div>
    )
}

export default UserDropDown;